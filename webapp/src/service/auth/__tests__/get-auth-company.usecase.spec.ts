import { describe, it, expect } from "vitest";

describe("Feature: getAuthenticateCompanyUsecase", () => {
  it("Birdy get authenticated success", async () => {
    // await whenCompanyAuthenticates();
    // thenCompanyShouldBeAuthenticated({
    //   name: "birady",
    // });
  });
});
